Example of a simple web portal

1. Create a reverse proxy for sFlow-RT metrics under path /dash/metric/
cp sflow-rt.conf /etc/httpd/conf.d
apachectl restart

2. Copy files to web root
cp -r dash /var/www/html/

3. Access dashboard at http://localhost/dash/

